# ==============================================================================
# APPLICATION SETTINGS
# ==============================================================================
# Ollama server base URL
OLLAMA_BASE_URL=http://127.0.0.1:11434

# Model name to use
OLLAMA_MODEL=qwen3:8b

# ==============================================================================
# WEB SERVER SETTINGS
#
# These are used by docker-compose.override.yml to configure the development
# server. See the README for more details on how to use them.
# ==============================================================================
# The IP address for the web service to bind to.
# Use 127.0.0.1 (default) to only allow access from the host machine.
# Use 0.0.0.0 to allow access from other devices on the same network.
WEB_BIND_HOST=127.0.0.1

# The port for the web service to listen on.
# This must be within the valid port range (1024-65535).
# The application will fail to start if this port is already in use.
WEB_PORT=8000

# ==============================================================================
# POSTGRESQL DATABASE SETTINGS
#
# These are used by the 'db' and 'api' services in docker-compose.yml.
# The DATABASE_URL is the connection string for the application.
# ==============================================================================
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=pvt-llm-api
DATABASE_URL=postgresql+psycopg://user:password@db:5432/pvt-llm-api

# ==============================================================================
# TEST DATABASE SETTINGS
#
# This is used by the 'db_test' service in docker-compose.override.yml.
# ==============================================================================
POSTGRES_DB_TEST=pvt-llm-api-test
