# OllamaサーバーのベースURL
OLLAMA_BASE_URL=http://127.0.0.1:11434

# 使用するモデル名
OLLAMA_MODEL=qwen3:8b

# PostgreSQL Database settings
# -----------------------------
# psycopg3 (psycopg v3) を使用するための接続文字列形式。
# DATABASE_URL=postgresql+psycopg://<user>:<password>@<host>:<port>/<dbname>
#
# Dockerコンテナ内からホストマシンで実行されているPostgreSQLに接続する場合の注意:
# Docker for Mac/Windows の場合、ホスト名は 'localhost' の代わりに 'host.docker.internal' を使用してください。
# 例: DATABASE_URL=postgresql+psycopg://user:password@host.docker.internal:5432/pvt-llm-api
#
# Linux の場合、`--network="host"` でコンテナを実行すれば 'localhost' が使えます。
# そうでない場合は、ホストのIPアドレスを直接指定する必要があります。
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/pvt-llm-api
